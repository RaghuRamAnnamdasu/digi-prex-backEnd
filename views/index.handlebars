<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        a {
            text-decoration: none;
        }
        img {
            max-width: 200px;
            max-height: 200px;
        }
    </style>
</head>

<body>
    <div>
        <h4>Hi User,</h4>
        <p>It seems that you have missed few items in your cart.</p>
        <div>{{name1}} and other items are waiting for you in the cart.</div>
        <div>
            <h2>{{name1}}</h2>
            <img src={{image1}} alt="{{name1}}" />
        </div>
        <div>
            <h2>{{name2}}</h2>
            <img src={{image2}} alt="{{name2}}" />
        </div>
        <div>
            <h2>{{name3}}</h2>
            <img src={{image3}} alt="{{name3}}" />
        </div>
        <div>
            <h2>{{name4}}</h2>
            <img src={{image4}} alt="{{name4}}" />
        </div>
        <button id="imageButton" onclick="onClick()"><a href={{link}} targer="blank">Go To Cart</a></button>
    </div>
</body>
</html>